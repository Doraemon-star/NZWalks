{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  // New ConnectionStrings object
  "ConnectionStrings": {
    // Key-value pairs
    // first string is the key which is the name of the ConnectionStrings
    // second string is the value, the server is the server name you installed, then specify the value of the db, EF will take to create the db
    "NZWalksConnectionString": "Server=LAPTOP-UST7NN5M;Database=NZWalksDb;Trusted_Connection=True;TrustServerCertificate=True"
  }
  }
